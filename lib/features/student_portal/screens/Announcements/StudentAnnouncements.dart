import 'package:flutter/material.dart';

class StudentAnnouncements extends StatelessWidget {
  const StudentAnnouncements({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Announcements"),
      ),
    );
  }
}
